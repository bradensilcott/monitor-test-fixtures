# Capture injection fixtures

Controlled test pages for URL Love It's change-detection ground-truth tests
(Linear URL-1645). Pages here are deliberately mutated between capture cycles;
every change is logged before it is made. Not a real store.
